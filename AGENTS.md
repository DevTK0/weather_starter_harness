# Agent skills

### Issue tracker

Issues and PRDs for this repo live as markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The canonical triage roles map directly to this repo's default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single root context. See `docs/agents/domain.md`.
